'use strict';

const permissions = {
    product: require('./product.permissions.json'),
    feedback: require('./feedback.permissions.json'),
    discount: require('./discount.permissions.json'),
};

module.exports = permissions;
