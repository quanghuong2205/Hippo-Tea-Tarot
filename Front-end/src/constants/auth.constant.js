'use strict';

const AUTH_MODES = {
    SIGN_IN: 'sign-in',
    SIGN_UP: 'sign-up',
};

const AUTH_FIELDS = {
    EMAIL: 'email',
    PASSWORD: 'password',
    CONFIRM_PASSWORD: 'confirm-password',
    NAME: 'name',
};

export { AUTH_MODES, AUTH_FIELDS };
